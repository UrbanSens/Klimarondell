// LSTSlider.js
import React, { useEffect } from 'react';
import {
  geoServerLayerSlider1, geoServerLayerSlider2, geoServerLayerSlider3,
  geoServerLayerSlider4, geoServerLayerSlider5, geoServerLayerSlider6,
  geoServerLayerSlider7, geoServerLayerSlider8, geoServerLayerSlider9,
  geoServerLayerSlider10, geoServerLayerSlider11, geoServerLayerSlider12
} from '../Map/LayerConfig';

// LSTSlider.js

export const rawLayers = [
    geoServerLayerSlider1, geoServerLayerSlider2, geoServerLayerSlider3,
    geoServerLayerSlider4, geoServerLayerSlider5, geoServerLayerSlider6
  ];
  
  export const smoothLayers = [
    geoServerLayerSlider7, geoServerLayerSlider8, geoServerLayerSlider9,
    geoServerLayerSlider10, geoServerLayerSlider11, geoServerLayerSlider12
  ];
  
  // Make sure to import or define the geoServerLayerSliderX variables accordingly.  
